# ---- Backup server connection ----
BACKUP_SERVER_USER="pi"              # can be empty if you use SSH config
BACKUP_SERVER_HOST="10.1.1.66"       # or a Host from ~/.ssh/config
BACKUP_SERVER_PORT="22"              

BACKUP_BASE_PATH="/mnt/backups"
REPO_NAME="os-laptop"

# ---- Target disk for restore (WILL BE WIPED) ----
TARGET_DISK="/dev/sda"           # new SSD/HDD, must be unformatted

# Which snapshot to restore (restic syntax: "latest" or ID)
SNAPSHOT="latest"

